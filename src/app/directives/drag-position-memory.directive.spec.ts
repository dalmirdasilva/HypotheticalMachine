import { DragPositionMemoryDirective } from './drag-position-memory.directive';

describe('DragPositionMemoryDirective', () => {
  it('should create an instance', () => {
    const directive = new DragPositionMemoryDirective();
    expect(directive).toBeTruthy();
  });
});
