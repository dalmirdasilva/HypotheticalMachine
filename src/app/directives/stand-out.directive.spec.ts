import { StandOutDirective } from './stand-out.directive';

describe('StandOutDirective', () => {
  it('should create an instance', () => {
    const directive = new StandOutDirective();
    expect(directive).toBeTruthy();
  });
});
